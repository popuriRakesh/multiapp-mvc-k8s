FROM mcr.microsoft.com/dotnet/sdk:9.0

WORKDIR /app
COPY . .

# ✔ required for dotnet watch inside Docker
ENV DOTNET_USE_POLLING_FILE_WATCHER=1

EXPOSE 8080

CMD ["dotnet", "watch", "run", "--urls", "http://0.0.0.0:8080"]
